# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json

# Node (only relevant once Eleventy/build tooling is added)
node_modules/
package-lock.json

# Local env/secrets, never commit real keys
.env
.env.local

# Build output, if a generator is added later
_site/
dist/
